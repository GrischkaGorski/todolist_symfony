{% extends 'tasks/layout.html.twig' %}

{% block title %}Tasks Index{% endblock %}

{% dump todos %}
{% block page_contents %}
    <div {{ react_component("Test", { 'test': 'coucou' } )}}>
        Chargement
    </div>
    =
    <a href="{{ path('test') }}">Coucou</a>
    {% for todo in todos %}
        {{ dump(todo) }}
        <ul>
            <li>
                <div>
                    {% if todo.done %}
                        <p>Fait</p>
                    {% else %}
                        <p>À faire</p>
                    {% endif %}

                    <p>{{ todo.title }}</p>
                </div>
                {% for tag in todo.tags %}
                    <p>{{ tag.name }}</p>
                {% endfor %}
                <div>===</div>
            </li>
        </ul>
    {% endfor %}
{% endblock %}
